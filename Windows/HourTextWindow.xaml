<Window x:Class="HourTextWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Teksten" Height="560" Width="560"
        WindowStartupLocation="CenterOwner" MinWidth="440" MinHeight="420"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource ForegroundBrush}">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Dag:" VerticalAlignment="Center" Margin="0,0,8,0" />
            <ComboBox x:Name="CboDay" Width="160" SelectionChanged="OnDayChanged" />
        </StackPanel>

        <DataGrid x:Name="Grid" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False"
                  CanUserDeleteRows="False" HeadersVisibility="Column" GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Uur" Binding="{Binding HourLabel}" IsReadOnly="True" Width="80" />
                <DataGridTextColumn Header="Tekst" Binding="{Binding Text, UpdateSourceTrigger=PropertyChanged}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Bewaren" Click="OnSave" Margin="0,0,8,0" IsDefault="True" />
            <Button Content="Sluiten" Click="OnClose" IsCancel="True"
                    Background="{DynamicResource ControlBackgroundBrush}"
                    Foreground="{DynamicResource ForegroundBrush}" />
        </StackPanel>
    </Grid>
</Window>
